"use strict";
function isString(dato) {
    return typeof dato === 'string';
}
const PrintData = (age) => {
    if (isString(age)) {
        console.log('es un string');
    }
    else {
        console.log('es un string');
    }
};
